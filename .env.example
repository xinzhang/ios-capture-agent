# OCR Mode Configuration
# Options: local (Tesseract.js) | llm (GPT-4 Vision)
# - local: Works offline, no API key needed, less accurate
# - llm: Requires internet and API key, highly accurate
OCR_MODE=local

# OpenAI API Configuration (required only when OCR_MODE=llm)
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Override default model settings (only for llm mode)
# OPENAI_MODEL=gpt-4o
# OPENAI_MAX_TOKENS=1000
# OPENAI_TEMPERATURE=0.3
